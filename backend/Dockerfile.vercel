FROM eclipse-temurin:17-jdk

WORKDIR /app

COPY . .

RUN chmod +x mvnw && ./mvnw clean package -DskipTests

# Vercel provides the PORT environment variable.
# Spring Boot must listen on that port.
CMD ["sh", "-c", "java -Dserver.port=${PORT:-80} -jar target/backend-0.0.1-SNAPSHOT.jar"]